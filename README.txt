the command to install the required packages: pip install -r requirements.txt
the command to run the example: python3 project2.py

This is an example for those who are not familiar with the development of the machine learning process. You can use any toolkit to implement your project. Scikit-learn and pytorch can be replaced by whatever you want.
